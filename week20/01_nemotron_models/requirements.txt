# Week 20 · App 1 — Nemotron Open Models (interactive tutorial)
#   uv pip install -r week20/01_nemotron_models/requirements.txt
#
# Dual-mode (auto-detected): REAL against a Nemotron endpoint (Ollama / NIM / vLLM
# on a DGX you point DGX_CONN/DGX_BASE_URL at) or SIM with no GPU. No cloud cost.
#   ollama run nemotron-3-nano:30b-a3b        # or a NIM: nvcr.io/nim/nvidia/nemotron-3-*
openai>=1.40
fastapi>=0.110
uvicorn>=0.29
