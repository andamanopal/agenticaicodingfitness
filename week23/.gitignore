# Week 19 — generated / runtime artifacts (never commit these)
**/.sandbox/
**/.memory/
**/__pycache__/
*.pyc
.venv/
# saved connection / SSH settings — may contain tokens & creds, never commit
**/.settings.json
# source deck photos (NVIDIA slides + call screenshots) — reference only, not for distribution
reference/

# Week 23 additions — never commit these
.DS_Store
# raw generated source assets (108 MB) — keep only the curated jpgs + intro video
media/originals/
# session debugging screenshots
issues/
.run_history.json
progress.json

# local tunnel env with a private tailnet hostname — copy from connect-remote.env.example
connect-remote.env
